Actualizar modulo para BD
    sudo docker compose up -d --build
    sudo docker exec -it odoo  odoo -d dbOdoo -u estate --stop-after-init
    sudo docker exec -it nombre_contenedor_odoo odoo -u nombre_de_tu_modulo -d nombre_de_tu_base_de_datos
conectar a odoo
docker exec -it odoo bash
conectar a la BD
    sudo docker exec -it dbOdoo  psql -U odoo -d dbOdoo
ejecutar query desde docker
    sudo docker exec -it dbOdoo  psql -U odoo -d dbOdoo -c "SELECT name, state FROM ir_module_module WHERE name = 'estate';"
    sudo docker exec -it dbOdoo  psql -U odoo -d dbOdoo -c "SELECT * FROM estate_property;"
    
dar permiso a carpeta
     sudo chmod a+rwx abc.py
obtener modulos base
    docker cp odooCp:/usr/lib/python3/dist-packages/odoo/addons ./odoo_base_modules
Ejecutar comando dentro de un contenedor
    sudo docker exec -it odoo ls
Por si la pantalla queda blanca
    debug=assets
capturar log
    docker logs -f nombre_contenedor_odoo
Reserved Fields

https://www.odoo.com/documentation/17.0/th/developer/tutorials/server_framework_101/10_constraints.html